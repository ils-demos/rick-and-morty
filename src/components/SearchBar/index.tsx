import * as React from "react";
import ISearchBar from "./index.i";

export default class SearchBar extends React.Component<ISearchBar>{
    constructor(props: ISearchBar){
        super(props)
    }

    render() {
        return(
            <div>
                Hi
            </div>
        )
    }
}